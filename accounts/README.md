# Accounts

Each subfolder = one account.

Structure:
  accounts/
    cxfan/          ← already live
    account2/
    account3/
    ...

Each account has:
  - session.json    ← Chrome auth state (gitignored)
  - persona.json    ← persona config
